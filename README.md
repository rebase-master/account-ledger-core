# Account Ledger Core (in-memory)

Append-only, value-dated, multi-currency account ledger core. No web layer, no persistence, no dependencies.

Run: 
   1. `npm test` (suite)
   2. `npm run replay` (prints per-day closing balance, fees, authorization states, errors for Day 1–6).