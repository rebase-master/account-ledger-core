# Worklog (IST)

- 2026-08-19 21:35 — Read the brief. Chose TypeScript on Node 22 (built-in type stripping + node:test), zero deps; no framework — the brief forbids a web layer and a DI container would be noise. Starting design: state model, then a hand replay of the stream before any code.
- 2026-08-20 09:18 — Updated NUMBERS.md, AMBIGUITIES.md, REJECTED.md with the findings. Reviewed docs with pairing partner. Added constants + rounding mode (half-up) to NUMBERS.md. Added ambiguity #3 (available-balance basis). Next: hand-replay Days 4-6 to rule on criteria 2 & 6 and settle the interest/fee ordering.
- 2026-08-20 14:59 — Hand-replayed the reversal path and ruled two criteria: criterion 6 rejected (E9 restores the ledger balance, but the ledger is append-only so fees already booked stay — balances return, fees don't); criterion 5 accepted but vacuous (Auth-B is declined, available -155, so "if Auth-B is approved" never fires). Committed.
- 2026-08-20 15:12 — Moved the judged-wrong criteria (2, 6) into REJECTED.md alongside 7 and 8, and recorded that all 8 were evaluated (1/3/4/5 accepted). Split the pasted brief out of README into ASSIGNMENT.md (verbatim, criteria numbered 1-8 so REJECTED's "Criterion N" refs line up) and slimmed README to a run/read guide.
- 2026-08-20 15:33 — Applied an independent review: tightened criterion 2 (three fee-days not one; assessment happens end of Day 5), collapsed the accepted-criteria list to a one-line completeness footer + added an abandoned-approaches slot, moved the instalment-split policy into NUMBERS.md with the "two rounding schemes, on purpose" rationale, added ambiguities #4 (partial settlement releases the whole hold) and #5 (overdraft-fee currency for a BHD account) plus a replay-order caveat on #2, and fixed typos.
- 2026-08-20 15:38 — Ruled the two material design decisions and documented them (AMBIGUITIES #6, #7): overdraft fee value_date = the negative day (fees compound; textual support = "value_date equal to the day assessed" + criterion 1); daily interest accrued as-known / point-in-time and immutable (support = append-only rule + criterion 1's "evaluated at end of Day 5"). Design phase complete; next is the engine + node:test suite that turns these rulings into assertions.
- 2026-08-20 17:54 — Created feat/ledger-engine branch. TDD on money.ts: toMinor (decimal string -> integer minor units), digit-wise, never parseFloat. 7 tests (AED/BHD happy paths, no-decimal input, short-fraction padding, zero, excess-decimals throw). Two real bugs caught mid-implementation: split on wrong delimiter (comma vs dot) after already stripping commas, and backwards/unpadded arithmetic (multiplying the fraction instead of concatenating padded digit strings) -- both surfaced by the tests as NaN before being fixed. Committed (3a64ba5), pushed. Next: formatMinor (fils -> display string) for the replay output.
- 2026-08-20 18:21 — formatMinor (fils -> display string): left-pad + slice, no division (division can lose precision; toMinor already proved exact scaling by digit concatenation, so unscaling stays symmetric). 3 tests (AED, BHD, small-amount left-pad boundary: 5 fils -> '0.05'). money.ts: 10/10. Committed (bec1384), pushed.
- 2026-08-20 18:45 — Opened PR #1 (money.ts + tests) for independent review.
- 2026-08-20 18:55 — PR #1 review: formatMinor breaks on negative sub-unit values (padStart pads left of the minus sign: -5 -> '0-5' -> '0.-5' instead of '-0.05'). Confirmed by writing failing tests first (TDD): -5/-50 AED and -5 BHD failed as predicted, -37000/-15500 (multi-unit negatives) already passed, matching the review's exact scoping. Fixed with sign-magnitude: strip sign, pad Math.abs(minor), reapply sign to the output. 13/13 green. Committed (87e6151), merged PR #1 (00b7fcf).
- 2026-08-20 18:56 — types.ts (LedgerEvent union, Entry, Hold, AccountState) and ledger.ts (closingLedgerBalance, availableBalance, applyEntry, authorize, settle) with 9 tests named after the real event shapes (E3/E5/E6/E8), asserting the exact fils numbers hand-derived earlier (250/50 after E3, ledger unchanged by an approved auth, -155/-245 decline at E8, 650->465 at E5, E6 posts nothing). 19/19 green, tsc --strict clean. Stripped inline comments to near-zero per house style (test names + function names carry the meaning; kept only the ledger-vs-available rule and the settle-releases-the-whole-hold note). Committed, pushed. Opened PR #2 for review.
- 2026-08-20 19:09 — PR #2 review: no critical issues; independently re-verified each of the 5 confirmed-correct claims against the code before acting on the rest. Two real (non-blocking) findings fixed: redundant active-hold filter in authorize() (availableBalance already filters), and HoldState included a 'declined' value no code ever assigns (a decline creates no Hold at all) -- removed it and renamed the one test whose name overclaimed testing it. 22/22 green, tsc clean. Two findings deferred as documentation decisions, not code: over-settlement/currency-mismatch guards (belongs in the Part 2 auth-lifecycle section) and formatMinor's negative-value fix (already merged via PR #1, so out of scope here). Committed (ab5f1f4), merged PR #2 (54fa411).
- 2026-08-20 19:44 — feat/fee-interest branch (off main, PRs #1/#2 merged). Implemented AMBIGUITIES #6/#7 as code: roundMinor (money.ts, ties toward +inf, only correct as used since interest is positive-balance-only), assessOverdraftFee (idempotent per day, valueDate = the negative day itself), accrueInterest (as-known point-in-time, positive balances only, no entry posted), capitalizeInterest (single Day-6 credit = sum of daily accruals, which is the code-level refutation of criterion 8 — nothing to discard, the total IS the sum). 8 new tests, 30/30 green, tsc clean.
